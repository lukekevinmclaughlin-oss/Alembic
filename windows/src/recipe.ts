import { limits } from './model';
import { type NormalizeOptions, standard } from './normalize';
import { type QualityRules, standardQuality, type PIIKind, piiKinds } from './quality';
import { type ChunkConfig, defaultChunk } from './chunker';
import type { ColumnType } from './types';
import { parseExpression } from './expression';
import { parseLossless, JSONNumber, stringifyLossless, type JSONValue } from './lossless-json';
export const augmentKinds=['generateQA','judgeScore','rewrite','classify','preferencePair'] as const;
export type AugmentConfig={kind:typeof augmentKinds[number];column:string;instruction:string;labels:string[];concurrency:number;maxTokens:number};
export type Op = {kind:'selectColumns'|'dropColumns'|'unifyNulls'|'dedupeExact';columns:string[]} | {kind:'renameColumn';from:string;to:string} | {kind:'addColumn';name:string;expression:string} | {kind:'filterRows';expression:string} | {kind:'normalizeText';columns:string[];options:NormalizeOptions} | {kind:'autoType'} | {kind:'coerceType';column:string;type:ColumnType} | {kind:'dedupeFuzzy';column:string;threshold:number} | {kind:'redactPII';columns:string[];kinds:PIIKind[];mode:'tag'|'hash'|'remove'} | {kind:'qualityFilter';column:string;rules:QualityRules} | {kind:'languageFilter';column:string;allowed:string[];minConfidence:number} | {kind:'decontaminate';column:string;evalTexts:string[];nGramSize:number} | {kind:'addTokenCount'|'addLanguage'|'addQualityScore';column:string} | {kind:'chunkText';column:string;config:ChunkConfig} | {kind:'split';train:number;validation:number;test:number;seed:string;stratifyBy:string|null} | {kind:'augment';config:AugmentConfig};
export type Step={id:string;enabled:boolean;op:Op};
export const titles:Record<Op['kind'],string>={selectColumns:'Select columns',dropColumns:'Drop columns',renameColumn:'Rename column',addColumn:'Add computed column',filterRows:'Filter rows',normalizeText:'Normalize text',unifyNulls:'Unify nulls',autoType:'Auto-detect types',coerceType:'Coerce type',dedupeExact:'Exact duplicates',dedupeFuzzy:'Near-duplicates',redactPII:'Redact PII & secrets',qualityFilter:'Quality filter',languageFilter:'Language filter',decontaminate:'Decontaminate vs evaluation set',addTokenCount:'Token count',addLanguage:'Language',addQualityScore:'Quality score',chunkText:'Chunk for RAG',split:'Train / validation / test split',augment:'Optional augmentation'};
function object(x:unknown):Record<string,unknown>{if(!x||typeof x!=='object'||Array.isArray(x))throw Error('Expected an operation object.');return x as Record<string,unknown>;}
function str(x:unknown,max=300,empty=false):string{if(typeof x!=='string'||x.length>max||(!empty&&!x.trim()))throw Error(`Expected ${empty?'a':'a nonempty'} string of at most ${max} characters.`);return x;}
function list(x:unknown,max=200,itemMax=300):string[]{if(!Array.isArray(x)||x.length>max)throw Error('Invalid list length.');return x.map(v=>str(v,itemMax));}
function num(x:unknown,min:number,max:number,integer=false):number{if(typeof x!=='number'||!Number.isFinite(x)||x<min||x>max||(integer&&!Number.isInteger(x)))throw Error(`Expected ${integer?'an integer':'a number'} between ${min} and ${max}.`);return x;}
function choice<T extends string>(x:unknown,choices:readonly T[]):T{if(typeof x!=='string'||!choices.includes(x as T))throw Error('Unsupported operation option.');return x as T;}
function bool(x:unknown):boolean{if(typeof x!=='boolean')throw Error('Expected a boolean.');return x;}
export function validateOp(input:unknown):Op{let x=object(input);if(!Object.hasOwn(x,'kind')){const keys=Object.keys(x);if(keys.length!==1)throw Error('Invalid Mac recipe operation.');x={...object(x[keys[0]]),kind:keys[0]};}const kind=choice(x.kind,Object.keys(titles) as Op['kind'][]),columns=()=>{const c=list(x.columns??[]);if(new Set(c).size!==c.length)throw Error('Repeated column selection.');return c;},column=()=>str(x.column);
  switch(kind){case'selectColumns':case'dropColumns':case'unifyNulls':case'dedupeExact':return{kind,columns:columns()};case'renameColumn':return{kind,from:str(x.from),to:str(x.to)};case'addColumn':case'filterRows':{const expression=str(x.expression,10000);parseExpression(expression);return kind==='filterRows'?{kind,expression}:{kind,name:str(x.name),expression};}case'normalizeText':{const raw={...standard,...object(x.options??{})},options={...standard};for(const key of Object.keys(standard) as (keyof NormalizeOptions)[]){if(key==='unicodeForm')options[key]=choice(raw[key],['none','nfc','nfkc']);else options[key]=bool(raw[key]);}return{kind,columns:columns(),options};}case'autoType':return{kind};case'coerceType':return{kind,column:column(),type:choice(x.type,['string','int','double','bool','date'])};case'dedupeFuzzy':return{kind,column:column(),threshold:num(x.threshold,.5,1)};case'redactPII':return{kind,columns:columns(),kinds:list(x.kinds??[],8).map(k=>choice(k,piiKinds)),mode:choice(x.mode,['tag','hash','remove'])};case'qualityFilter':{const raw={...standardQuality,...object(x.rules??{})},rules={...standardQuality};for(const key of Object.keys(standardQuality) as (keyof QualityRules)[]){if(key==='flagTruncated'||key==='requireTerminalPunctuation')rules[key]=bool(raw[key]);else rules[key]=num(raw[key],0,key==='maxWords'?100000:key==='minWords'?100000:key.includes('WordLength')?100:1,key==='minWords'||key==='maxWords');}if(rules.minWords>rules.maxWords||rules.minMeanWordLength>rules.maxMeanWordLength)throw Error('Quality minimum exceeds maximum.');return{kind,column:column(),rules};}case'languageFilter':{const allowed=list(x.allowed,30,8);if(allowed.some(c=>!/^[a-z]{2,3}$/.test(c)))throw Error('Invalid language code.');return{kind,column:column(),allowed,minConfidence:num(x.minConfidence,0,1)};}case'decontaminate':{const evalTexts=list(x.evalTexts,100,limits.cellChars);if(evalTexts.reduce((n,s)=>n+s.length,0)>4_000_000)throw Error('Evaluation text exceeds four million characters.');return{kind,column:column(),evalTexts,nGramSize:num(x.nGramSize,1,20,true)};}case'addTokenCount':case'addLanguage':case'addQualityScore':return{kind,column:column()};case'chunkText':{const r={...defaultChunk,...object(x.config??{})},config:ChunkConfig={targetTokens:num(r.targetTokens,32,8192,true),overlapTokens:num(r.overlapTokens,0,8191,true),minChunkTokens:num(r.minChunkTokens,0,8192,true),respectMarkdown:bool(r.respectMarkdown),includeHeadingContext:bool(r.includeHeadingContext)};if(config.overlapTokens>=config.targetTokens||config.minChunkTokens>config.targetTokens)throw Error('Overlap/minimum must fit the target chunk budget.');return{kind,column:column(),config};}case'split':{const train=num(x.train,0,1),validation=num(x.validation,0,1),test=num(x.test,0,1);if(Math.abs(train+validation+test-1)>1e-9)throw Error('Split fractions must sum to one.');if(typeof x.seed==='number'&&!Number.isSafeInteger(x.seed))throw Error('Large seeds must be preserved as decimal strings.');const seed=String(x.seed);if(!/^\d{1,20}$/.test(seed)||BigInt(seed)>(1n<<64n)-1n)throw Error('Seed must be an unsigned 64-bit integer.');return{kind,train,validation,test,seed,stratifyBy:x.stratifyBy==null?null:str(x.stratifyBy)};}case'augment':{const r=object(x.config),labels=list(r.labels??[],100,100);if(new Set(labels).size!==labels.length)throw Error('Duplicate classification labels.');return{kind,config:{kind:choice(r.kind,augmentKinds),column:str(r.column),instruction:str(r.instruction??'',4000,true),labels,concurrency:num(r.concurrency??4,1,16,true),maxTokens:num(r.maxTokens??1024,128,8192,true)}};}}
}
function plain(v:JSONValue):unknown{if(v instanceof JSONNumber){const n=Number(v.raw);if(!Number.isFinite(n))throw Error('Recipe number is not finite.');return /^-?\d+$/.test(v.raw)&&!Number.isSafeInteger(n)?v.raw:n;}if(Array.isArray(v))return v.map(plain);if(v!==null&&typeof v==='object'){const o:Record<string,unknown>=Object.create(null);for(const [k,x]of Object.entries(v))o[k]=plain(x);return o;}return v;}
export function parseRecipe(text:string):{name:string;createdAt:string;ops:Op[]}{if(text.length>8_000_000)throw Error('Recipe exceeds 8 million characters.');const x=object(plain(parseLossless(text)));if(x.version!==1)throw Error('Unsupported recipe version.');if(!Array.isArray(x.ops)||x.ops.length>limits.steps)throw Error('Recipe exceeds 100 steps.');const createdAt=str(x.createdAt,40);if(!Number.isFinite(Date.parse(createdAt)))throw Error('Invalid recipe date.');return{name:str(x.name,300),createdAt,ops:x.ops.map(validateOp)};}
export function exportRecipe(name:string,steps:Step[],createdAt=new Date().toISOString()):string{const ops=steps.filter(s=>s.enabled).map(s=>{const {kind,...args}=validateOp(s.op);const params=args as unknown as Record<string,JSONValue>;if(kind==='split')params.seed=new JSONNumber(String(params.seed));return{[kind]:params};});return stringifyLossless({version:1,name,createdAt,ops})+'\n';}
export function makeStep(op:Op):Step{return{id:crypto.randomUUID(),enabled:true,op:validateOp(op)};}
